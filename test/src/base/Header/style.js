import vars from "../../styles/vars.js";

const style = {
    header: {
        position: "fixed",
        top: "0",
        left: "0",
        right: "0",
        zIndex: 1000,
        ...vars.header,
    }
}

export default style;