import vars from "../../styles/vars.js";

const style = {
    footer: {
        marginTop: `10px`,
        padding: `10px`,
        marginLeft: vars.aside.width,
        marginRight: vars.aside.width,
        ...vars.footer,
    }
}

export default style;