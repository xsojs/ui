import theme from "../../styles/theme";

const styles = {
    codeBlock: {
        padding: '20px',
        fontFamily: 'Roboto Mono',
        overflow: 'auto',
        '> *': {
            fontFamily: 'Roboto Mono',
        }
    }
};

export default styles;